package com.demolition.beginningcompose.models

data class UserProfile( val imgId: Int,
                        val imgDesc: String,
                        val name: String,
                        val status: String)
